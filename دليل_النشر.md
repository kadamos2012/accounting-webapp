# دليل تشغيل ونشر نظام المحاسبة (النسخة الأونلاين)

## 📌 الوضع الحالي (مرحلة أولى - جاري البناء)
الصفحات الجاهزة دلوقتي:
- ✅ تسجيل الدخول
- ✅ الصفحة الرئيسية (لوحة معلومات)
- ✅ استيراد تقديمات اليوم
- ✅ تقرير حساب الوكلاء

**لسه هيتضاف:** باقي التقارير، تسجيل الخزنة، الغياب، تعديل الأسعار... (زي البرنامج المكتبي بالظبط).

---

## 1) تجربة البرنامج على جهازك أول (قبل النشر)

### التثبيت
```
pip install -r requirements.txt
```

### الإعداد
احفظي رابط الاتصال بتاع Supabase (اللي جبتيه من Settings > Database) فى متغيرات البيئة. على ويندوز، فى Command Prompt:
```
set DATABASE_URL=postgresql://postgres:كلمة_السر_الحقيقية@db.pqqlezchslloqpirklrj.supabase.co:5432/postgres
set APP_PASSWORD=اختاري_كلمة_سر_للدخول_للبرنامج
set SECRET_KEY=أي_نص_عشوائي_طويل
```

### التشغيل
```
python app.py
```
افتحي المتصفح على: **http://localhost:5000**

---

## 2) النشر على Render (عشان يبقى Online فعليًا)

### الخطوة 1: رفع الكود على GitHub
1. اعملي حساب على **github.com** لو مفيش عندك
2. اعملي **repository جديد** (خاص/Private أفضل، لأنه بيانات شركة)
3. ارفعي كل ملفات المجلد ده عليه (فيه شرح لو محتاجة: github.com/new)

### الخطوة 2: ربط GitHub بـ Render
1. روحي **render.com** وسجّلي دخول
2. دوسي **"New +"** → **"Web Service"**
3. اختاري **"Build and deploy from a Git repository"**
4. اربطي حساب GitHub بتاعك واختاري الـ repository اللي رفعتيه

### الخطوة 3: إعدادات الخدمة
- **Name**: أي اسم (مثلاً accounting-system)
- **Region**: أقرب منطقة ليكي
- **Build Command**: `pip install -r requirements.txt`
- **Start Command**: `gunicorn app:app`
- **Instance Type**: Free

### الخطوة 4: متغيرات البيئة (مهم جدًا)
فى نفس صفحة الإعداد، تحت **"Environment Variables"**، ضيفي:
| الاسم | القيمة |
|---|---|
| `DATABASE_URL` | رابط الاتصال الكامل من Supabase (بكلمة السر الحقيقية) |
| `APP_PASSWORD` | كلمة السر اللي عايزة تدخلي بيها للبرنامج |
| `SECRET_KEY` | أي نص عشوائي طويل (مثلاً: `a8f5f167f44f4964e6c998dee827110c`) |

### الخطوة 5: النشر
دوسي **"Create Web Service"** - استني 2-3 دقايق لحد ما يخلص البناء.

بعد ما يخلص، هيديكي **رابط حقيقي** (زي `https://accounting-system.onrender.com`) - ده اللي تفتحيه من أي جهاز/موبايل فى أي مكان.

⚠️ **ملحوظة:** فى الباقة المجانية، لو البرنامج ما اتفتحش لمدة 15 دقيقة، بيقفل تلقائيًا ("ينام") - وأول فتحة بعد كده بتاخد 30-60 ثانية عشان يصحى. ده طبيعي فى الباقة المجانية.

---

## اختبرت كل حاجة قبل ما أبعتها

- شغّلت السيرفر فعليًا (gunicorn) وتأكدت إنه بيفتح صح
- اختبرت تسجيل الدخول، الصفحة الرئيسية، والاستيراد بملف تجريبي حقيقي - كل حاجة اشتغلت واتسجلت صح فى قاعدة البيانات
- اختبرت تقرير الوكلاء وطلع بالأرقام الصحيحة

**الخطوة الجاية:** أكمل بناء باقي الصفحات (تسجيل الخزنة، باقي التقارير...) فى الرسايل الجاية.
