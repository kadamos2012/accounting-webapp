-- ============================================================
-- migration_add_gender.sql
-- شغّليه فى Supabase -> SQL Editor -> Run (مرة واحدة بس)
-- بيضيف حقل الجنس للمستخدمين، عشان رسائل النظام تخاطب كل مستخدم بصيغة
-- مذكر أو مؤنث صح حسب جنسه، بدل صيغة واحدة ثابتة للكل.
-- ============================================================

ALTER TABLE users ADD COLUMN IF NOT EXISTS gender TEXT DEFAULT 'female';
