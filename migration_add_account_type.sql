-- ============================================================
-- migration_add_account_type.sql
-- شغّليه فى Supabase -> SQL Editor -> Run (مرة واحدة بس)
-- بيضيف نوع الحساب (نقدي / بنك) لكل حركة خزنة، عشان يتحسب رصيد منفصل
-- لكل واحد بدل ما يكونوا مخلوطين فى رصيد واحد. الحركات القديمة كلها
-- هتتحط "نقدي" افتراضيًا (تقدري تعدّليها لاحقًا لو محتاجة).
-- ============================================================

ALTER TABLE treasury ADD COLUMN IF NOT EXISTS account_type TEXT DEFAULT 'نقدي';
